# Bull Flip 策略收敛决策（2026-09-03）

## 结论

正式策略入口收敛到完整日线 `bull_flip`。趋势中的回踩重新走强只保留为观察标签，
不再授予正式技术买入资格；压缩突破继续保持纸面实验，不允许实盘执行。

组合执行以 `core90_ma200_bull10` 作为唯一主策略。现有 RiskParity、Theme Alpha、
BTC satellite 等纸面账户不删除，继续后台积累前向对照记录，但不再作为并列主策略展示。

## 证据范围

决策使用冻结的 xquant 长样本研究资产：

- 样本：2015-01-01 至 2026-07-10，预热从 2013-01-02 开始；
- 候选池：81 个本地可观察标的，按月滚动 PIT 资格和流动性排名；
- 信号：SuperTrend 7/3 完整日线空翻多；
- 执行：信号后各标的下一有效 Open；
- 退出：首次完整日线 ST 翻空后的下一有效 Open；
- 成本：单边 5 bps 手续费加 5 bps 不利滑点；
- 约束：单标的 10%、最多 10 个并发、bull sleeve 最大 100% 敞口。

来源：`/Users/zz/Desktop/Code/zsd/xquant/outputs/scan-v2-all-bull-flip-long-sample/`
以及经开盘价定仓修正的
`/Users/zz/Desktop/Code/zsd/xquant/outputs/scan-v2-all-bull-flip-defense-open-sizing-fix-2026-08-20/`。

## 三个问题如何处理

| 问题 | 原始 bull flip | 收敛方案 | 长样本结果 |
|---|---:|---|---:|
| 最大回撤偏深 | -36.77% | 分市场 MA200 只暂停新开仓 | -21.81% |
| 恢复期过长 | 1,016 天 | MA200 准入；已有仓位仍按自身 ST 退出 | 364 天 |
| 利润集中 | 前 5 个盈利标的贡献约 52% | Bull 仅占总组合 10%，单标的占总 NAV 约 1% | Core90 + MA200 Bull10 回撤 -13.49% |

最终主策略 `core90_ma200_bull10` 的长样本指标为：CAGR 约 16.28%、Sharpe 约
1.43、最大回撤约 -13.49%、最长恢复 283 天。相比原始 Core90 + Bull10，CAGR
基本不变，最大回撤从约 -15.03% 降低到 -13.49%。

这里没有通过删除历史赢家来“修复”集中度。收益集中本身不可预测，采用卫星预算和
单标的风险上限更可复现，也更不容易产生幸存者偏差。

`fe35d3b` 对 512890 单独把回踩 ADX 从 25 降到 20，缺少策略级回测支持，也会引入
明显的单标的过拟合。近五年本地回放中，这个例外额外引入两笔已完成亏损交易
（约 -1.46% 和 -1.93%），没有形成稳定改善。因此该硬编码已经删除，回踩整体降为
观察，不再通过继续调低阈值补救。

## 冻结执行合同

1. 只接受新的完整日线 `bull_flip`，不追买 MA200 防守期间错过的旧信号。
2. MA200 只控制新开仓；已有仓位继续等待自身完整日线 ST 翻空退出。
3. 回踩、ADX 特例、MACD 和周线状态不得替代或补发 bull flip。
4. 信号、选池、成本、执行时点和仓位规则不得依据后续历史表现回填修改。
5. 其他纸面账户继续运行，只用于前向比较，不改变主策略权限。

## 市场代表日期保护

`281ad60` 增加的代表品种完整日期审计继续保留，但不同日期不再直接阻断市场模式。
两个有效代表会回放到最近共同完整交易日，并用当时可见的历史月线方向计算模式。
响应同时记录各代表相对最新行情的自然日差和已完成时段差；统一允许落后 1 个已完成
时段，超过后才转为 `insufficient`。该规则对所有市场一致，不设置单标的例外。

## 仍需前向验证

长样本仍不是未来保证。候选目录不是完整历史证券主表，仍缺少退市、清盘和从未进入
本地缓存的失败标的；也未完整模拟税费、涨跌停、市场冲击、ETF 溢价和汇率。

因此本次只收敛纸面主策略，不扩大实盘授权。优先观察 MA200 是否继续降低亏损月、
回撤深度和恢复时间；若连续前向记录不支持这些改善，应复核而不是继续增加过滤器。
