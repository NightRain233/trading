[project]
name = "backend"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = [
    "fastapi>=0.128.0",
    "pandas>=2.3.3",
    "pandas-ta>=0.4.71b0",
    "pyarrow>=23.0.0",
    "uvicorn>=0.40.0",
    "yfinance>=1.1.0",
]

[dependency-groups]
dev = [
    "pytest>=9.0.3",
]
